package com.tpdb.interfaceadapter.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class BeanConfig {

    // In this file we configure all non repo, service components we need to config
}
